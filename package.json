{
  "name": "luke-fashion-root",
  "version": "1.0.0",
  "description": "Root workspace workspace runner for Luke Fashion full stack project",
  "scripts": {
    "start": "npm run server --prefix backend",
    "backend": "npm run server --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "seed": "npm run data:import --prefix backend",
    "destroy": "npm run data:destroy --prefix backend"
  }
}
